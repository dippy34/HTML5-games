services:
  - type: web
    name: nova-hub
    env: node
    rootDir: .
    buildCommand: npm install
    startCommand: npm start
    skipSubmodules: true
    envVars:
      - key: NODE_ENV
        value: production
