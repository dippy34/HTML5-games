# Cloudflare Pages configuration
# For Pages projects, only pages_build_output_dir is needed
pages_build_output_dir = "."

# Note: For Workers deployment, use a separate wrangler.toml or remove this file
# Workers configuration (commented out for Pages):
# name = "buh-avon"
# main = "src/index.js"
# compatibility_date = "2024-01-01"
# routes = [
#   { pattern = "buh-avon.bestboymg1.workers.dev", zone_name = "bestboymg1.workers.dev" }
# ]

