[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm install -g serve"]

[phases.build]
cmds = ["echo 'Static site - no build needed'"]

[start]
cmd = "serve -s . -l ${PORT:-3000}"

